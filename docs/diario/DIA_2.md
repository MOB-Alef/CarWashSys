# 🚗 CarWashSys

## 📅 Dia 2

### Objetivos concluídos

- Configuração dos templates.
- Configuração dos arquivos Static.
- Configuração dos arquivos Media.
- Criação do base.html.
- Criação dos componentes reutilizáveis.
- Criação da primeira página do sistema.
- Configuração das URLs do Dashboard.
- Primeira execução completa do CarWashSys.

### Resultado

O sistema deixou de utilizar a página padrão do Django e passou a exibir sua própria identidade.

A arquitetura de templates foi validada com sucesso.

Todos os testes (`check`, `migrate` e `runserver`) foram concluídos sem erros de configuração.

### Observação

O único aviso restante refere-se ao arquivo `style.css`, que será criado posteriormente.